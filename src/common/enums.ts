export enum Plan {
  FREE = 'FREE',
  PRO = 'PRO',
}

export enum CheckStatus {
  UP = 'UP',
  DOWN = 'DOWN',
  SLOW = 'SLOW',
  UNKNOWN = 'UNKNOWN',
}

export enum AlertType {
  DOWN = 'DOWN',
  RECOVERED = 'RECOVERED',
  SLOW = 'SLOW',
}
